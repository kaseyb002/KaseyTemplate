import Foundation

public struct HelpViewRequiredData {
    let isModallyPresented: Bool
    
    public init(
        isModallyPresented: Bool
    ) {
        self.isModallyPresented = isModallyPresented
    }
}
